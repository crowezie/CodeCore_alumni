<%= simple_form_for @profile, remote: true do |p| %>
  <%= p.simple_fields_for :skills, @profile.skills.build do |f| %>
  <%= f.input :name, label: "Name", placeholder: 'e.g. Ruby on Rails' %>
  <%= f.input :level, label: "Level", placeholder: '1 - 10' %>
  <% end %>
  <ul class="list-inline pull-right">
  <li><button type="button" class="btn btn-default prev-step">Previous</button></li>
  <li><button type="button" class="btn btn-default next-step">Skip</button></li>
  <li>
  <%= p.button :submit, "Save and continue", class: "btn btn-primary"%>
  </li>
  <%end%>
  </ul>
